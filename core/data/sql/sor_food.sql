CREATE TABLE IF NOT EXISTS sor_math (
    parental_level_of_education VARCHAR(255),
    lunch VARCHAR(255),
    test_preparation_course VARCHAR(255),
    math_score INT,
    reading_score INT,
    writing_score INT
);